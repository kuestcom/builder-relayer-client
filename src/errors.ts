export const CONFIG_UNSUPPORTED_ON_CHAIN = new Error("contract config unsupported on chain");
export const SIGNER_UNAVAILABLE = new Error("signer unavailable");
export const BUILDER_CREDS_UNAVAILABLE = new Error(
    "builder credentials are required for this endpoint",
);
